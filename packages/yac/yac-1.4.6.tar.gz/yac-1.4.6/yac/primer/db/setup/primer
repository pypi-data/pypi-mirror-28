
Your Atlassian Cloud (YAC) - db/setup

Available comands include:

  yac db setup (-h | primer)

-h provides api help
primer will display this page

The db setup command is used to setup a DB on the RDS instance in your stack.

Setup includes:
* creating the DB, and
* creating a DB user/role for your app to connect with

If the DB already exists, db setup will wipe the existing DB and recreate.
