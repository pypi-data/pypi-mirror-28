#!/usr/bin/env python3
# -*- coding:utf-8 -*-

from jam import lookup
import sys

if __name__ == "__main__":
    if len(sys.argv) == 2:
        lookup(sys.argv[1])
    else:
        progname = sys.argv[0].split('/')[-1]
        print("Usage: %s COMMAND\n  e.g: %s ANA" % (progname, progname))
