# 描述: Python是一种高级编程语言.

# Python内置简单的web服务器
# 开启之后通过浏览器访问http://127.0.0.1:8000

# Python 2.7版本
python -m SimpleHTTPServer
# Python 3.x版本
python -m http.server 8000

# 用于Debug的SMTP服务器, 消息将会被丢弃, 并打印到标准输出.
python -m smtpd -n -c DebuggingServer localhost:1025

# json输出美化
python -mjson.tool
