# 统计单词个数(文件或标准输入)
wc -w /path/to/foo.txt
cat /path/to/foo.txt | wc -w

# 统计文本行数(文本或标准输入)
wc -l /path/to/foo.txt
cat /path/to/foo.txt | wc -l

# 统计字节数(文件或标准输入)
wc -c /path/to/foo.txt
cat /path/to/foo.txt | wc -c

# 统计当前目录下的文件个数
ls -l | wc -l

# 如果你曾在一个shell脚本中使用`wc` ，
# 且需要将其结果和一个整数进行比对。
# 可以使用xargs清洗输出:
ls -l | wc -l | xargs
