# 仅下载一个文件
# URL可以是http(s), ftp, .torrent 种子文件或一个磁力链接
aria2c <url>

# 阻止下载 .torrent 种子文件
aria2c --follow-torrent=mem <url>

# 从一个文件读取url (-i)
# 持续下载一个文件的分部(-c)[断点续传]
# 到制定本目录 (-d)
# 一次下载一个文件(-j)
# reading urls from the file (-i)
aria2c -j 1 -c -d ~/Downloads -i /path/to/file
