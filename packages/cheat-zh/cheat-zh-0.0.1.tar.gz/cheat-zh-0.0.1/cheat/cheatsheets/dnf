# 安装一个包的最新版本:
dnf install <包名>

# 根据给定的字符串搜索包详情
dnf search <字符串>

# 寻找哪个包提供指定的二进制文件
dnf provides <path to binary>

# 安装"dnf-plugins-core"之后下面是可用的

# 下载一个包
dnf download <package name>

# 安装SRPM或一个.spec文件提供的构建依赖
# install the build dependencies for a SRPM or from a .spec file
dnf builddep <srpm/.spec file>
