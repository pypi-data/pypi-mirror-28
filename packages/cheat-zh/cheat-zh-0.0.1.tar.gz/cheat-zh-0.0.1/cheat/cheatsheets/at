# 调度一次性任务
at {time}
{command 0}
{command 1}
Ctrl-d

# {time} 可以是下面的格式：
now | midnight | noon | teatime (4pm)
HH:MM
now + N {minutes | hours | days | weeks}
MM/DD/YY

# 列出在队的任务
atq

# 移除一个任务 (使用atq中的id)
atrm {id}
