============
Installation
============

At the command line::

    $ easy_install django-price-level

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-price-level
    $ pip install django-price-level
