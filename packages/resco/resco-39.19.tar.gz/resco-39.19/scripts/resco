#!/usr/bin/env python
"""
Usage:
    resco start-project <name>
"""

from docopt import docopt
from resco import cli

if __name__ == '__main__':
    args = docopt(__doc__)
    cli.start_project(args['<name>'])
