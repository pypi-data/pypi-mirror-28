from setuptools import setup

setup(name='wamp-connection',
      version='0.1',
      description='Simple to use library to handle a WAMP connection',
      author='GeorgeWilson96',
      packages=['connection'],
     )
