from distutils.core import setup
from setuptools import find_packages

setup(
    name = 'ui_to_py',
    version ='1.1',
    packages = find_packages(),
    author = 'featTheB',
    long_description=open(r'README.md').read(),
    url = 'https://github.com/featTheB/ui-to-py-gui',
    
    )