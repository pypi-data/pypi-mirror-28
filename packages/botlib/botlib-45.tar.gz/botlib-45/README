.. raw: <html>

    <br><br>

BOTLIB is a python3 framework you can use to program bots. It makes it 
possible to program your own module enabling your own commands and contains serveral modules that can be used to program bots.

BOTLIB has a MIT no-clause license.

install
=======

you might need to do the following if the bot doesn't work:

::

  export PYTHONPATH="."
  export PYTHONIOENCODING="utf-8"

download
========

clone the source:

:: 

  hg clone https://bitbucket.org/bthate/botlib2
  cd botlib2
  sudo python3 setup.py install --user

another option is to download with pip3:

::

  pip3 install botlib --upgrade

programming
===========

user programmed modules are read from the ~/.mods directory, you can put your .py files over there.

example:

::

  def hi(event):
      event.reply("hi %s" % event.origin)

modules
=======

::

    the following modules/packages are part of botlib:

    bot.cli
    bot.clock
    bot.cmds
    bot.cmds.clock
    bot.cmds.db
    bot.cmds.doc
    bot.cmds.ed
    bot.cmds.email
    bot.cmds.input
    bot.cmds.license
    bot.cmds.loader
    bot.cmds.oper
    bot.cmds.rss
    bot.cmds.test
    bot.cmds.udp
    bot.cmds.users
    bot.core
    bot.db
    bot.dcc
    bot.defines
    bot.event
    bot.fleet
    bot.handler
    bot.irc
    bot.loader
    bot.obj
    bot.raw
    bot.rss
    bot.select
    bot.space
    bot.thr
    bot.users
    bot.utils
    bot.utils.db
    bot.utils.decorators
    bot.utils.log
    bot.utils.misc
    bot.utils.shell
    bot.utils.time
    bot.utils.trace
    bot.utils.url
    bot.xmpp

contact
=======

you can email me or contact me on IRC/freenode/#dunkbots.

| Bart Thate (bthate@dds.nl, thatebart@gmail.com)
| botfather on #dunkbots irc.freenode.net
