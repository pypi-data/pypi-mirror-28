# throestl-hash-python

This package implements the hashing algorithm used by Dallar.

## Usage

```python
    import throestl_hash
    data = '\x00'
    digest = throestl_hash.getHash(data, len(data))
```
