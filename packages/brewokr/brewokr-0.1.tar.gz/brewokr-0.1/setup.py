from distutils.core import setup

setup(
    name = 'brewokr',
    packages = ['brewokr'], # this must be the same as the name above
    version = '0.1',
    description = 'A simple library for Brewokr',
    author = 'Bre',
    author_email = 'brewokr@gmail.com',
    url = 'https://github.com/brewokr/pybrewokr', # use the URL to the github repo
    download_url = 'https://github.com/brewokr/pybrewokr/archive/0.1.tar.gz', # I'll explain this in a second
    keywords = ['simple', 'brewokr', 'library'], # arbitrary keywords
    classifiers = [],
)